spring:
  cloud:
    nacos:
      discovery:
        name: ${spring.application.name}
        server-addr: 10.116.1.15:8848
        namespace: server13
      config:
        server-addr: 10.116.1.15:8848
        namespace: server13
        group: DEFAULT_GROUP
        name: ${spring.application.name}
        file-extension: yml
        refresh: true
        shared-configs:
          - data-id: application.yml
            refresh: true