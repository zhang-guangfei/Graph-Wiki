server:
  port: 8022
spring:
  application:
    name: wm-service
  profiles:
    active: test

  zipkin:
    base-url: ${zipkinBaseUrl}
  main:
    allow-bean-definition-overriding: true
    allow-circular-references: true