spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: prod
      config:
        server-addr: localhost:8848
        namespace: prod
        group: DEFAULT_GROUP
        name: ${spring.application.name}
        file-extension: yml
        refresh: true
        shared-configs:
          - data-id: application.yml