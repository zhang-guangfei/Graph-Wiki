// import { findParentListByPositionName } from '@/api/user'
// export default{
//   inserted(el, binding, vnode) {
//     const condition = binding.value
//     findParentListByPositionName(condition).then((data) => {
//       const operatorId = condition.operatorId
//       for (const key in data) {
//         if (data[key].psnsmcId === operatorId) {
//           return true
//         }
//       }
//       el.parentNode && el.parentNode.removeChild(el)
//     })
//   }
// }
